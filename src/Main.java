
public class Main {
    public static void main(String[] args) {
        // Declarar un arreglo de números enteros
        int[] numeros = {1, 2, 3, 4, 5};

        // Usar un ciclo for para recorrer el arreglo
        for (int i = 0; i < numeros.length; i++) {
            System.out.println(numeros[i]);
        }
    }
}